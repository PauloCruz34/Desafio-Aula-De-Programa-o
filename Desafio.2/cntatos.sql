create database contatos;

use contatos;

create table contatos(
	email varchar(30),
    assunto varchar(60),
    descricao varchar(200)
    
    );

select * from contatos  #seleciona todos os dados na tabela contatos

